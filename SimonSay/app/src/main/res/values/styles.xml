<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/CustomActivityAnimation </item>
        <item name="android:statusBarColor">@color/cardview_dark_background </item>
    </style>

    <style name="CustomActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/fade_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_exit</item>
    </style>
    <style name="Slide">
        <item name="android:windowEnterAnimation">@anim/animate_custom_dialog</item>
        <item name="android:windowExitAnimation">@anim/animate_custom_dialogexit</item>
    </style>

</resources>
